%li
  %span.content= event.name
  %span.timestamp
    Posted #{time_ago_in_words(event.created_at)} ago.
  - if current_user?(event.user)
    = link_to "delete", event, method: :delete, |
      data: { confirm: "You sure?" },           |
      title: event.name                         |
